on:
  # Workflows check plugin Jeedom
  push:
    branches:
      - master
      - beta
  pull_request:
    branches:
      - beta
      - master
      
name : 'Full workflow Plugin Jeedom'

jobs:
  plugin:
    uses: jeedom/workflows/.github/workflows/plugin.yml@main
